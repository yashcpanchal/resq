# ResQ-Capital API
fastapi
uvicorn[standard]
python-dotenv
pydantic
httpx
tiktoken

# Layer 3: Embeddings and briefing synthesis via OpenRouter (no Gemini).
# Set OPENROUTER_API_KEY in .env. Optional: OPENROUTER_EMBED_MODEL, OPENROUTER_CHAT_MODEL.

# Actian VectorAI (optional): install from project wheel when using the vector DB.
# pip install actian-beta/actiancortex-0.1.0b1-py3-none-any.whl
