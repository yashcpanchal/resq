# ResQ-Capital API
fastapi
uvicorn[standard]
python-dotenv
pydantic
httpx
tiktoken

# Embeddings and synthesis use Gemini REST API via httpx (no SDK).
# Set GEMINI_API_KEY in .env.

# Actian VectorAI (optional): install from project wheel when using vector DB.
# pip install actian-beta/actiancortex-0.1.0b1-py3-none-any.whl
