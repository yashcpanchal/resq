# Requirements for RAG Example
# Install with: pip install -r examples/requirements_rag.txt

# Core embedding model (required)
sentence-transformers>=2.2.0

# Optional: For full RAG with answer generation
openai>=1.0.0

# Note: The base cortex client is installed from the wheel file
# pip install actiancortex-0.1.0b1-py3-none-any.whl
